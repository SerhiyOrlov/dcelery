version: '3.8'

services:
  django:
    container_name: django
    build:
      context: ./app
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./app:/usr/src/app/
    ports:
      - '8001:8000'
    environment:
      - DEBUG=1
      - SECRET_KEY=django-insecure-1#pd*by#=pdhy1#q*4o9vjpcr+wi@38ay2%$r5-6^o5$iw^61)
      - ALLOWED_HOSTS=localhost,127.0.0.1